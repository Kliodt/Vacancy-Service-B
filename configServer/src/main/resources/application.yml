server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kliodt/vacancy-app-config
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/config"