server:
  port: 8082

spring:
  application:
    name: user-service
  
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update #todo: update -> validate
  
  # flyway:
  #   enabled: true
  #   url: ${DB_URL}
  #   user: ${DB_USER}
  #   password: ${DB_PASS}

